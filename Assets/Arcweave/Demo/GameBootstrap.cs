namespace Arcweave
{
    // Cross-scene state: main menu sets these before loading the game scene.
    public static class GameBootstrap
    {
        public static bool ShouldLoadSave;
        public static int LoadSlot;
    }
}
